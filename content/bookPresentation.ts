import type { BookId, ThemeId } from "./schema";

/** Редакционный слой «Витрины» (§9 «книги как объекты»): как книга ПОДАЁТСЯ.
 *  Держим ОТДЕЛЬНО от канона content/books/*.json — там метаданные (год, состав,
 *  художник), здесь арт-дирекшен разворота. Тот же приём, что у content/anchors.ts:
 *  декор не подмешивается в zod-схему контента.
 *
 *  Фразы издателя — сжатие docs/концепция.md §2 (строки 56-57), не выдумка.
 *  Курсив размечен `*…*` и рендерится тем же renderInline, что и проза «Читальни».
 *
 *  Темы — РЕДАКЦИОННЫЙ набор (тоже §2 концепции), а не топ по частоте themes[]:
 *  частотный счёт даёт «Человек» и «Душу» в обеих книгах, и книги перестают
 *  различаться. Финальное слово по составу — за автором. */
export type BookPresentation = {
  skin: "manila" | "concrete"; // фактура обложки-макета
  mirror: boolean; // зеркальная композиция разворота (сведения слева)
  glyph: boolean; // монограмма-кружок на обложке
  pubsay: string; // фраза издателя; `*…*` → курсив
  themes: readonly ThemeId[]; // на каких понятиях «Колеса» стоит книга
};

/** Только изданные книги: у третьей («kniga3») подача принципиально иная —
 *  не объект, а стопка черновиков, и живёт она прямо в BooksSection. */
export const bookPresentation: Partial<Record<BookId, BookPresentation>> = {
  kolizey: {
    skin: "manila",
    mirror: false,
    glyph: false,
    pubsay:
      "Гуманистические притчи о свете и тьме — *магический реализм в повседневности*: озорной луч дружит с мальчиком, а зло всегда прикладывает инструкцию.",
    themes: ["light", "soul", "choice"],
  },
  koleso: {
    skin: "concrete",
    mirror: true,
    glyph: true,
    pubsay:
      "По слову издателя — *«офисная готика»*: серые и скучные области жизни, намеренно расцвеченные в фантастические цвета. Смерть здесь — усталый персонаж, честно делающий свою работу.",
    themes: ["illusion", "death", "rebirth"],
  },
};
