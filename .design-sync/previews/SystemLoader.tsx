// Авторская превью SystemLoader — Suspense-фолбэк маршрута (§6). Сцена «офис ночью»:
// компонент несёт .dark сам, поэтому рендерим его full-bleed, без светлой рамки.
// Натриевая сканирующая черта — CSS-анимация (под reduced-motion статична), кадр может
// поймать её в любой фазе: это норма, а не нестабильность.
import { SystemLoader } from "kirilov";

// Дефолтная реплика загрузки — то, что видно при входе на «Витрину».
export const Boot = () => <SystemLoader />;

// Регистр под контекст: «Читальня» называет, что именно открывается.
export const Story = () => <SystemLoader label="Открываем рассказ" />;
