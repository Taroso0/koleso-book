// Авторская превью NotFoundScene — 404 «голосом одержимой системы» (§6): «404» разрядом
// (машина), реплика серифом (душа), тёплое окно на периферии — то самое боковое зрение.
// Сцена несёт .dark сама, поэтому рендерится full-bleed, без светлой рамки.
//
// play={false}: разряд GlitchText длится 1.8 с, а капчер ждёт только шрифты и картинки —
// кадр поймал бы «404» на середине материализации. Живая страница играет разряд как обычно.
import { NotFoundScene } from "kirilov";

export const Scene = () => <NotFoundScene play={false} />;
