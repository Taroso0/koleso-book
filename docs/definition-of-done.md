# Definition of Done (§13) — статус

Прогон чек-листа §13 тех-спецификации после Фазы 6. Легенда: ✅ закрыто кодом ·
🟡 частично/нужен ручной прогон · ⛔ не закрыто (ручная задача автора/отдельный шаг).

> Ревизия 2026-07-21 (архитектурный аудит). Предыдущая — 26.06; за месяц документ
> разошёлся с кодом: §4 и §6 числили закрытыми не то, что закрыто на самом деле.

## 1. Reduced-motion-паритет ✅
Полноценный статичный опыт при reduced (ОС или тумблер): зерно/курсор/зачин/перестройка
«Колеса»/Lenis — все off; у каждого эффекта есть статичный сценарий. Шаги 4.x/6.1.

## 2. Доступность ✅ (живой SR-прогон — 🟡 за человеком)
WCAG AA по контрасту — **измерено** (Шаг 6.1: muted-foreground 6.2:1; фокус `--foreground`
≥3:1; opacity-текст убран). «Колесо» с клавиатуры (roving tabindex + стрелки), индекс-двойник
`WheelIndex` (канон), видимый фокус, ARIA, skip-link. **Ручное:** прогнать NVDA/VoiceOver.

## 3. Кириллица ✅
Проза/UI/«»/тире/ударения рендерятся в вариативных шрифтах (сабсет cyrillic, swap,
авто-fallback — без FOUT). Подтверждено в `docs/проверка-в-браузере.md` и на OG-карточках.

## 4. Скролл ✅
Lenis только в «Витрине»; «Читальня» — нативный скролл; якоря/восстановление позиции
работают. Три из четырёх багов постраничного режима закрыты в `3f218f3`; остаток
(1 кадр движения при F5, рассказ короче экрана) — в `docs/отложенное.md`.

## 5. Производительность 🟡 (бюджеты превышены)
SSG всего контента ✅; WebGL/граф/SplitText — ленивы и изолированы ✅ (проверено грепом);
AVIF + React Compiler ✅ (Шаг 6.2). ISR не нужен — контент считается на сборке.
**Бюджеты 🟡:** замер 2026-07-21 — `/read` 783 КБ и `/read/[book]/[story]` 802 КБ при
потолке 700 (+12…15 %); `/` и `/workshop` под потолком. Причина — рост framework-слоя.
Автоматического гейта на размер бандла нет, Lighthouse/CWV не измерялись
(метод и пороги — `docs/перформанс.md`).

## 6. Контент 🟡 (вычитка — ручное)
Извлечён из PDF, иллюстрации привязаны ✅; метаданные + динамические OG ✅ (Шаг 6.3).
- ✅ **`themes[]` проставлены (34/34)** — коммит `a18ca79`; граф «Колеса» даёт 96 рёбер.
- ⛔ Вычитка 34 рассказов (0/34, `docs/proofreading-checklist.md`).
  ⛔ Имена иллюстраторов (`book.artist`). ⛔ Реальные контакты.

## 7. Архитектура ✅
Две среды разделены (route-группы `(vitrina)`/`(chitalnya)`); дип-линки «Колесо»→«Читальня»
(`WheelIndex`/граф → `/read/...`); переходы между группами стабильны.

---

## Опциональные улучшения (НЕ требуются §13, не сделаны)
- **View Transitions** (§6/§E2) узел→страница — задел на будущее.
- **Pagefind** (поиск) — опционально (§2).
- **Подписи иллюстраторов**, **sameAs автора** в JSON-LD — за данными автора.

## Перед продакшеном (деплой)
- Задать **`NEXT_PUBLIC_SITE_URL`** = реальный домен (сейчас плейсхолдер `kirilov.example`
  в metadataBase/sitemap/OG). Без него OG/canonical/sitemap указывают на плейсхолдер.
- Прогнать Lighthouse/CWV; проверить соц-превью (OG) реальным URL.
