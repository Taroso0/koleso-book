# Журнал изменений

Это понятный, человеческим языком, дневник всех правок в проекте. Он нужен,
чтобы владелец проекта мог следить за тем, что меняется, **не читая код**.
Технический список правок есть в истории Git (сообщения коммитов), а здесь —
подробные пояснения простыми словами: что сделали, зачем и как это проверить.

> Заполняет этот файл Claude (ИИ-ассистент) после каждой задачи, в которой
> менялся код или настройки. Новые записи добавляются **сверху**.

---

## Как читать запись

Каждая запись построена по одной схеме:

- **Что изменилось** — простыми словами, без терминов: что стало работать иначе.
- **Зачем** — какую задачу или проблему это решает.
- **Технические подробности** — какие файлы и как затронуты (для разработчика;
  можно пропустить, если не вникаешь в код).
- **Как проверить** — что открыть/нажать, чтобы убедиться, что всё работает.

Если какой-то пункт не применим к правке — он опускается.

---

## 2026-07-15 — Устранены последние 2 предупреждения линтера (граф «Колеса»)

**Что изменилось:**
Довели до конца прошлую задачу: два оставшихся «замечания» в визуальном графе
«Колеса» тоже устранены. Теперь внутренняя проверка кода полностью чистая — **0
замечаний** (было 8). Ускоритель React (компилятор) снова оптимизирует граф —
раньше он «пропускал» этот компонент из-за спорного места в коде.

**Зачем:**
Полностью чистый baseline: любое новое замечание сразу видно и не тонет среди
«известных». Плюс граф «Колеса» вернулся под авто-оптимизацию — меньше лишних
перерисовок при наведении/фокусе.

**Технические подробности:**
- `components/wheel/WheelCanvas.tsx`: расчёт раскладки при наведении/фокусе на
  рассказ (`displayLayout`) больше не обёрнут в ручной `useMemo` — мемоизацию
  отдали React Compiler (он не мог сохранить ручную: считает, что d3-раскладка
  «мутирует» результат `reweight`, растягивая область его зависимостей через
  границу `useMemo`). Плоский `const` такой границы не создаёт. Заодно «граф
  внимания» строится от исходного графа + множества прочитанного, а не от
  memoized-слоя памяти, — так дорогая укладка памяти (200 итераций) НЕ
  пересчитывается на каждый hover.
- Значение раскладки идентично прежнему (чистые детерминированные функции), граф
  «Колеса» и его тесты (`lib/graph.ts`, `lib/wheelLayout.ts`, 177 тестов) не
  тронуты и зелёные.
- Baseline линтера в `CLAUDE.md` и навыке `/verify` снижен с 2 до 0.

**Как проверить:**
Поведение не менялось. На десктопе открыть главную, найти граф «Колеса», навести/
сфокусировать рассказ — подсветка, карточка-превью и «живое внимание» работают как
прежде, плавно. Линт (0 ошибок), типы, 177 тестов и сборка — зелёные.

---

## 2026-07-15 — Устранены 6 из 8 технических предупреждений линтера

**Что изменилось:**
Внутренняя проверка кода (линтер) давно показывала 8 «замечаний» — сигналов о
местах, где код написан не по свежим рекомендациям React. На вид сайта они не
влияли, но накапливались как техдолг. Шесть из них устранены: код в этих местах
переписан на современный лад. Два оставшихся — в визуальном графе «Колеса» —
осознанно оставлены: это не поломка (граф работает как прежде), а лишь отказ
ускорителя от авто-оптимизации одного компонента; их починка требует аккуратных
правок «рисковой» части графа и вынесена в отдельную задачу.

**Зачем:**
Меньше «шумных» замечаний — легче замечать действительно новые проблемы; код в
шести местах стал ближе к тому, как это делается в актуальном React (надёжнее при
будущих обновлениях).

**Технические подробности:**
- Новый хук `components/motion/useHydrated.ts` (на `useSyncExternalStore`) —
  штатная замена паттерну «прочитать клиентское значение после маунта через
  `useState`+`useEffect`».
- `react-hooks/refs` (2): `GlitchText.tsx`, `StoryOpening.tsx` — присваивание ref
  перенесено из тела рендера в `useEffect`.
- `react-hooks/set-state-in-effect` (4): `ContinueReading.tsx` и
  `useHauntedCapability.ts` переведены на `useHydrated`; `MotionProvider.tsx` —
  localStorage стал источником правды тумблера (без `setState`-в-effect);
  `ReaderShell.tsx` — восстановление позиции в постраничном режиме отложено в
  `requestAnimationFrame` (как в соседней ветке прокрутки).
- Baseline линтера снижен с 8 до 2 — обновлены `CLAUDE.md` и навык `/verify`.
- Граф «Колеса» (`WheelCanvas.tsx`, `lib/graph.ts`, `lib/wheelLayout.ts`) не
  тронут.

**Как проверить:**
Поведение не менялось. Стоит бегло глянуть: «Читальня» (режимы «Свиток»/«Страницы»,
возврат на прежнее место, темы), блок «Продолжить чтение», тумблер «снизить
эффекты» в подвале, появление рассказа при клике в «Колесе». Типы, линт (ровно 2
известные ошибки) и прод-сборка — зелёные.

---

## 2026-07-15 — Плавное разворачивание списков и карта частей сайта для проверки

**Что изменилось:**
1. Списки рассказов под темами «Колеса» теперь разворачиваются и сворачиваются
   **плавно** (мягко «выезжают» по высоте), а не рывком. Если у человека включён
   режим «меньше движения» — системная настройка ОС **или** тумблер «снизить
   эффекты» в подвале сайта — раскрытие снова мгновенное, без анимации.
2. Инструмент внутренних скриншотов (которым ассистент проверяет вид сайта, не
   дёргая владельца) получил **карту всех частей сайта**. Теперь можно назвать
   часть по имени (например, «список тем» или «панель настроек чтения») — и снимок
   нужного места получается с первого раза, без угадывания. Заодно можно
   «нажать» на элемент перед снимком (например, раскрыть тему) — это раньше
   требовало каждый раз писать отдельный скрипт.

**Зачем:** плавность делает разворачивание приятнее и понятнее (видно, что список
именно раскрывается), а мгновенный вариант под «меньше движения» — это правило
проекта (у каждой анимации есть спокойный статичный сценарий). Карта частей —
чтобы проверка вида сайта была быстрой и надёжной: в прошлый раз ассистент
ошибся селектором и снял не тот блок.

**Технические подробности:**
- Плавность — чистый CSS через `::details-content` + `interpolate-size` в
  `app/globals.css` (класс `.wheel-disclosure`): прогрессивное улучшение —
  где браузер не поддерживает, раскрытие просто мгновенное, без поломок.
  `WheelIndex` остаётся серверным компонентом (без JavaScript).
- Чтобы ручной тумблер «снизить эффекты» тоже отключал анимацию, `MotionProvider`
  теперь вешает на `<html>` атрибут `data-reduce-effects`; на него опираются
  чистые CSS-эффекты (у ОС-настройки для этого есть свой `@media`).
- Скрипт `element-shot.mjs` (навык `/verify-in-browser`): добавлены каталог частей
  `--part <имя>`, `--list` (печать карты), `--click` (нажать перед снимком) и
  ожидание монтирования клиентских островов. Карта продублирована в `SKILL.md`.

**Как проверить:** на главной пролистать до «Колеса», кликать по темам — списки
разворачиваются плавно. Включить в подвале «Снизить эффекты» (или системный режим
уменьшения движения) — раскрытие станет мгновенным.

## 2026-07-15 — Списки «Колеса» сворачиваются по клику

**Что изменилось:** в списке тем на главной («Колесо») рассказы под каждой темой
теперь спрятаны по умолчанию. Видны только названия тем со счётчиком рассказов и
стрелочкой-указателем. Нажатие на тему раскрывает её список рассказов (и разворачивает
стрелочку вверх), повторное нажатие — сворачивает. Так весь перечень из десяти тем
помещается на экран и не приходится прокручивать длинную «простыню».

**Зачем:** раньше все рассказы всех тем были раскрыты сразу — получался очень длинный
список, в котором тяжело охватить взглядом все темы. Теперь сначала виден компактный
обзор тем, а детали открываются по желанию.

**Технические подробности:** `components/wheel/WheelIndex.tsx` — каждая тема обёрнута
в нативный `<details>/<summary>` вместо всегда-открытой секции. Выбор осознанный: это
работает без JavaScript, полностью доступно с клавиатуры и для скринридера (важное
правило проекта — «Колесо» это каноничная доступная навигация), и компонент остаётся
серверным (без утяжеления страницы). Стрелка-шеврон плавно поворачивается на `rotate`
(с уважением к режиму «меньше движения»). Быстрые чипы-ссылки на темы сверху при
переходе сами раскрывают нужную тему. Тесты компонента и проверки типов/линта — зелёные.

**Как проверить:** открыть главную страницу, пролистать до блока «Колесо». Списки
рассказов свёрнуты; клик по названию темы раскрывает рассказы, повторный — прячет.

## 2026-07-15 — Автотесты и проверка на GitHub

**Что изменилось:** у проекта появился «автопилот проверки». Раньше правильность
кода проверялась руками; теперь есть 177 автоматических тестов, которые за пару
секунд перепроверяют всю «умную» часть сайта — карту «Колесо» (как рассказы
связываются с темами, как она «оседает» вокруг прочитанного), проверку контента
(что у рассказа, книги и записи «Мастерской» правильно заполнены поля), русскую
типографику, память чтения (прогресс, «прочитано», «продолжить чтение»),
переключатель «снизить эффекты» и определение слабых устройств. Эти проверки
теперь запускаются автоматически на GitHub при каждом изменении.

**Зачем:** чтобы будущие правки (особенно доработки внешнего вида) не ломали
незаметно логику. Если что-то сломается — проверка загорится красным сразу, а не
всплывёт позже у читателя. Крайние случаи (пустые данные, битые записи, границы
чисел) тоже под присмотром.

**Технические подробности:** поднят тест-раннер Vitest (+ jsdom, Testing Library),
файлы тестов — в `tests/`, конфиг — `vitest.config.ts`. Покрытие измеряется по
«слою логики» (`lib/`, схемы контента, загрузчики, интерактивные хуки и компоненты):
там ~98% строк и ~92% ветвлений, порог в CI — 90%. Визуальный и анимационный слой
намеренно не покрывается юнит-тестами (его проверяют в браузере). Добавлен GitHub
Actions `.github/workflows/ci.yml`: типы, тесты с покрытием, прод-сборка, линт.
Команды: `npm test`, `npm run test:coverage`, `npm run test:watch`.

**Как проверить:** `npm test` — все тесты зелёные; `npm run test:coverage` — отчёт
покрытия (HTML в `coverage/index.html`). На GitHub во вкладке **Actions** после
пуша у задачи CI появится зелёная галочка.

---

## 2026-07-15 — Навык проверки в браузере: снимает и то, что «ниже сгиба»

**Что изменилось:** помощник визуальной проверки (`/verify-in-browser`) научился
надёжно снимать состояния, которые не видны сразу на первом экране, — например граф
«Колесо» (он ниже героя) и его «живые» состояния (когда тема горит под вниманием).
Раньше из-за инерционного скролла Витрины такой снимок соскальзывал обратно наверх, на
заглавный экран.

**Зачем:** при проверке якорей на «Колесе» вскрылось, что обычный снимок до графа не
доходит, и это отняло время. Теперь техника закреплена в навыке — в будущих правках
проблема не повторится.

**Технические подробности:** новый скрипт `element-shot.mjs` (снимок одного элемента
независимо от скролла; «живое внимание» задаётся фокусом клавиатуры, а не мышью — hover
терялся при захвате) и раздел «2а» в `SKILL.md` навыка `verify-in-browser`.

**Как проверить:** `npm run dev`, затем
`node .claude/skills/verify-in-browser/element-shot.mjs --selector '[aria-label="Колесо тем"]'`
— в папке снимков появится кадр самого графа (а не героя).

---

## 2026-07-15 — Якоря-иллюстрации на «Колесе»

**Что изменилось:**
- На карте «Колесо» (только десктоп, окно шире 1024px) у трёх тем появились
  маленькие иллюстрации-«плашки» в рамке-паспарту, как плашки в «Читальне»:
  у «Человека» — «Наблюдатель», у «Смерти» — «Девять жизней», у «Иллюзии» — «Fake It».
- Плашка тиха в покое (полупрозрачна), набирает полную плотность, когда её тема
  «горит» (в покое всегда горит самая крупная тема — «Человек»; при наведении огонь
  переходит на тему под курсором), и гаснет вместе с темой, когда внимание на другой.
  Под живым вниманием чуть приподнимается — но только если не включён режим
  «снизить эффекты».
- Ни на что, кроме вида, не влияет: клики, подписи, укладка, память и клавиатура
  работают как раньше. На мобильном якорей нет (там и графа нет).

**Зачем:** карта смыслов была собрана только из света и типографики — оставалась
абстракцией. Пара якорей-образов даёт ей «плоть»: за них цепляется глаз и память
(концепция §8, §14).

**Технические подробности:** новые `content/anchors.ts` (карта тема→иллюстрация,
отдельно от канон-схемы тем), `lib/anchors.ts` (берёт размеры из
`content/illustrations.json`), `components/wheel/AnchorPlate.tsx` (плашка на
`next/image`, сама убирается при ошибке загрузки файла). Слой добавлен в
`components/wheel/WheelCanvas.tsx` — HTML-оверлей поверх SVG, позиция и состояние
берутся из тех же данных, что красят узлы (укладку/вес/память не трогает). Стили —
блок `.wheel-anchor*` в `app/globals.css`. Новых картинок не добавляли — размеры
плашек уже были в манифесте иллюстраций.

**Как проверить:** `npm run dev`, открыть `/` на десктопе (окно шире 1024px),
доскроллить до «ночного» Колеса. У «Человека» плашка яркая, у «Смерти» и «Иллюзии» —
приглушённые. Навести на «Смерть» → её плашка загорается и чуть приподнимается,
«Человек» гаснет. Включить «снизить эффекты» → подъёма нет, плашки на месте. Сузить
окно до < 1024px → графа и якорей нет, остаётся список тем.

---

## 2026-07-14 — Настроен ИИ-помощник: проверки, навыки и этот журнал

**Что изменилось:**
- У проекта появилась «обвязка» для Claude Code: обновлена главная инструкция
  помощника (CLAUDE.md) — теперь она отражает, что сайт построен и идут доработки.
- Появились три навыка: **/verify** (полная проверка: типы, линтер, сборка),
  **/verify-in-browser** (помощник сам открывает сайт в браузере и снимает
  скриншоты ключевых страниц и тем — раньше это делал владелец вручную)
  и **/handoff** (реализация дизайн-хендоффов из «Загрузок» по отработанной процедуре).
- После каждой правки кода линтер проверяет изменённый файл автоматически.
- Заведён этот журнал изменений.

**Зачем:** Визуальные проверки ложились на владельца (скриншоты руками, по
несколько заходов на одну правку), ошибки линтера разбирались заново в каждом
чате, а договорённости о процессе жили только в переписке. Теперь это
закреплено в проекте и работает в любом новом чате.

**Технические подробности:** `.claude/settings.json` (кодировка UTF-8 для Python,
список разрешённых команд, хук линтера), `.claude/hooks/lint-on-edit.mjs`,
навыки в `.claude/skills/` (verify, verify-in-browser + скрипт скриншотов,
handoff, changelog), переписан `CLAUDE.md`, создан `ИЗМЕНЕНИЯ.md`.

**Как проверить:** В новом чате Claude Code в папке проекта набрать `/verify` —
помощник прогонит проверки и отчитается; `/verify-in-browser` — поднимет сайт
и покажет скриншоты страниц в светлой/тёмной темах.

---

## 2026-07-10 — Тонкая настройка «Колеса» в состоянии покоя

**Что изменилось:**
- Размер тем на карте «Колесо» теперь точнее передаёт, сколько у каждой темы
  рассказов: крупные темы стали заметно крупнее мелких (раньше разница была
  почти незаметна).
- В состоянии покоя (когда никто не наводит курсор) главная тема на карте
  теперь слегка «тлеет» — видна, но неярко; при наведении она разгорается сильнее.
- Линии-связи между темами и рассказами стали чуть заметнее в спокойном
  состоянии — карта смыслов читается ещё до первого клика.

**Зачем:** после предыдущей доработки карта в покое выглядела однобоко — одна
тема «горела» на пол-экрана, а остальная сеть связей была почти не видна.
Нужна была более честная и спокойная картина «в тишине».

**Технические подробности:** `components/wheel/WheelCanvas.tsx` — радиус и
кегль темы теперь растут от корня числа рассказов (а не линейно), «тление» и
«полный огонь» разделены как разные состояния подсветки, базовая яркость
холодных рёбер поднята с 0.08 до 0.11. Раскладка узлов (`lib/wheelLayout.ts`)
не менялась.

**Как проверить:** открыть главную страницу, прокрутить до карты «Колесо»
(на широком экране) и посмотреть на неё, не трогая мышью — главная тема
должна тлеть, а не бросаться в глаза; при наведении — разгораться.

---

## 2026-07-09 — «Колесо» запомнило прочитанное, поправлен контраст страницы 404

**Что изменилось:**
- Карта «Колесо» теперь запоминает, какие рассказы посетитель уже прочитал:
  прочитанные рассказы и их темы получают заметное отличие на карте (тёплая
  заливка, кольцо, более яркая линия) — карта на глазах меняется по мере чтения.
- Поправлен контраст на странице «не найдено» (404): заголовок «404» и ссылка
  «вернуться к Колесу» раньше были практически не видны (тёмный текст на тёмном
  фоне) — теперь их хорошо видно.
- Устранено техническое предупреждение в консоли браузера, связанное со
  вступительной анимацией на главной странице (на то, что видит посетитель,
  не влияло, но мешало разработке).
- Внутренний экспорт дизайн-системы (используется только для работы с внешним
  инструментом claude.ai/design, на сайт не влияет) пополнен новыми компонентами.

**Зачем:** «память Колеса» — часть идеи сайта: карта смыслов должна расти
вместе с историей чтения, а не оставаться неизменной картинкой. Контраст 404 —
требование доступности (WCAG AA): единственная ссылка для выхода со страницы
не должна быть незаметной.

**Технические подробности:** `lib/graph.ts` (`reweightRead`; в записи прогресса
чтения добавлено монотонное поле `max` — раньше `pct` мог откатиться назад при
повторном открытии рассказа с начала и стереть «прочитанность»), `lib/wheelLayout.ts`
(наклон темы к центру пропорционально доле прочитанного); `app/globals.css`
(контраст `.notfound__code`/`.notfound__back`); пре-пейнт скрипт зачина перенесён
в корневой `app/layout.tsx`; `.design-sync/` — ещё 8 компонентов и шим `next/dynamic`.

**Как проверить:** почитать несколько рассказов в «Читальне», затем вернуться
на главную — их узлы и темы на карте «Колесо» должны визуально выделяться;
открыть несуществующий адрес сайта — заголовок «404» и ссылка должны быть
хорошо читаемы.

---

## 2026-07-08 — Первая строка на главной «оживает», у системных экранов появился свой голос

**Что изменилось:**
- При первом входе на главную страницу заголовок-«кикер» («03:14 · опенспейс
  пуст…») теперь появляется побуквенно, как будто печатается — раньше он сразу
  отображался целиком.
- У экрана загрузки, страницы 404 и пустых разделов (например, пока в
  «Мастерской» нет записей) появились собственные тексты в духе сайта —
  жутковатые и вместе с тем спокойные, вместо нейтральных технических подписей.
- Исправлено техническое предупреждение в консоли браузера и невидимый
  заголовок в превью карты «Колесо».

**Зачем:** сайт задуман как «одержимый» интерфейс — даже служебные экраны
(загрузка, 404, пустое состояние) должны говорить характерным голосом системы,
а не быть безликими техническими подписями.

**Технические подробности:** `components/vitrina/HeroZachin.tsx` (каскад букв
через `StoryOpening`/SplitText, гейт по `prefers-reduced-motion` и ручному
тумблеру), `content/systemVoice.ts` (единый источник трёх реплик), новые
`components/ui/EmptyState.tsx`, `components/vitrina/NotFoundScene.tsx` +
`app/not-found.tsx`, `SystemLoader`.

**Как проверить:** зайти на главную страницу сайта первым разом за сессию (или
в приватном окне) — строка над «тёплым окном» должна появляться печатающимся
эффектом; открыть несуществующий адрес — увидеть авторский текст 404; включить
«снизить движение» в настройках сайта — эффекты должны стать статичными без
потери текста.

---

## 2026-07-07 — Тизер «Колеса» на первом экране приведён в порядок

**Что изменилось:**
- Декоративное мини-созвездие в углу первого экрана (превью «Колеса», зовущее
  прокрутить вниз) было визуально «мусорным» скоплением линий — теперь это
  один аккуратный горящий путь до самой крупной темы, без хаоса.
- Обновлена внутренняя инструкция для ИИ-помощника (не видна на сайте) —
  актуальный список команд и карта устройства проекта.

**Зачем:** тизер должен приглашать к «Колесу», а не отвлекать шумом на первом
экране — это первое, что видит посетитель.

**Технические подробности:** `components/vitrina/WarmWindowHero.tsx`
(`buildHint` — один путь хаб→тема вместо случайных линий), `app/globals.css`
(`.wheel-hint::before` — фон-«тихая стена»). `CLAUDE.md` — обновлена документация.

**Как проверить:** открыть главную страницу, посмотреть на маленькое
созвездие в углу первого экрана — должен быть виден единый горящий путь,
а не мешанина линий.

---

## 2026-07-03 — «Созвездие» переехало в ночь, сшит стык с первым экраном

**Что изменилось:**
- Карта «Колесо» на главной странице (широкий экран) теперь показывается на
  тёмном, «ночном» фоне вместо светлого — натриевые огоньки тем и рассказов
  читаются гораздо лучше.
- Стык между тёплым окном на первом экране и тёмной картой ниже был заметен
  как шов (светлая полоса/уступ) — теперь переход сплошной, без границ.
- Мелкие правки: счётчик рассказов у темы больше не наезжает на подпись у
  крупных тем; рассказы-точки на карте стали чуть крупнее и лучше видны на
  тёмном фоне; мини-созвездие на первом экране перерисовано тем же языком,
  что и сама карта (вес тем, свечение).

**Зачем:** карта задумана как «созвездие смыслов» — тёмный фон нужен, чтобы
огоньки тем действительно выглядели звёздами, а не блёклыми точками на бумаге.

**Технические подробности:** `app/(vitrina)/page.tsx` + `app/globals.css`
(`.wheel-night`, `.dawn-strip` — полноширинная тёмная секция и градиент-
«рассвет» к светлой странице ниже; full-bleed без отступов и скруглений);
`components/wheel/ThemeNode.tsx`, `StoryNode.tsx`; `components/vitrina/WarmWindowHero.tsx`.

**Как проверить:** открыть главную страницу на широком экране, прокрутить от
первого экрана вниз до карты «Колесо» — переход в тёмную секцию должен быть
плавным, без светлых полос или углов.

---

## 2026-07-02 — Внутренняя техническая настройка (не видна на сайте)

**Что изменилось:** правка затрагивает только служебный экспорт дизайн-системы
в инструмент claude.ai/design — на сайт и на то, что видит посетитель, не влияет.

**Технические подробности:** `app/globals.css`, `.design-sync/NOTES.md` —
Tailwind по ошибке сканировал markdown-документацию и подхватывал упомянутые
в тексте классы как «используемые», из-за чего экспорт дизайн-системы менялся
без реальных изменений в коде; документация исключена из сканирования.

---

## 2026-07-01 — «Колесо» заговорило языком созвездия

**Что изменилось:**
- Карта тем и рассказов «Колесо» стала выразительнее: размер кружка темы
  теперь показывает, сколько у неё рассказов (раньше все были одинаковые),
  название темы отображается уже привычным «книжным» шрифтом.
- В состоянии покоя на карте теперь горит один путь — до самой крупной по
  числу рассказов темы (по расчёту — «Человек»), остальное — приглушённая сеть.
- Кружки тем «дышат» — едва заметно пульсируют, добавляя карте ощущение
  живой (для тех, кто не отключил анимации).

**Зачем:** прежняя версия карты выглядела как научная схема — равные точки,
серые линии, пустое состояние без акцентов. Задача сайта — карта смыслов
как «дом», а не диаграмма.

**Технические подробности:** `lib/graph.ts` (`themeDegree`),
`components/wheel/ThemeNode.tsx`, `WheelCanvas.tsx`, `app/globals.css`
(`@keyframes wheel-breathe`, гейт под `prefers-reduced-motion`, не более
3 вспышек/сек). Служебная правка того же дня — техническая доводка экспорта
дизайн-системы, на сайт не влияет.

**Как проверить:** открыть главную страницу (широкий экран), посмотреть на
карту «Колесо» — темы должны быть разного размера, один путь — гореть ярче
остальных, кружки тем — слегка пульсировать; включить «снизить движение» —
пульсация должна пропасть, подсветка — остаться.

---

## 2026-06-30 — Больше видимой фактуры на «Витрине», внутренние технические сверки

**Что изменилось:**
- «Витрина» (главная и соседние страницы, кроме «Читальни») стала визуально
  фактурнее: появилось заметное зерно, лёгкая «бетонная» и «бумажная» текстура
  фона — раньше поверхности выглядели гладкими и плоскими.
- Зерно и фактура видны даже с включённым «снизить движение» — двигаться
  перестаёт только анимация, а не сама текстура.
- Несколько технических сверок и правок внутреннего экспорта дизайн-системы
  (не видны на сайте).

**Зачем:** по концепции сайта («офисная готика») обыденность должна ощущаться
«настоящей» — фактура бетона и бумаги вместо гладкого цифрового фона.

**Технические подробности:** `components/haunted/GrainOverlay.tsx`,
`StaticGrain.tsx` (статичное зерно теперь видно всегда, включая
reduced-motion/слабые устройства — не только в полном режиме),
`app/globals.css` (`.vitrina-surface`, `.paper-surface`); контраст текста на
новых поверхностях проверен (AA). `.design-sync/` — сверка после обновления
инструмента claude.ai/design.

**Как проверить:** открыть главную страницу — фон должен выглядеть текстурным
(не гладким); включить «снизить движение» в настройках сайта — текстура
должна остаться, пропасть должна только анимация.

---

## 2026-06-29 — «Тёплое окно» на первом экране

**Что изменилось:**
- Первый экран «Витрины» (главная страница) вместо простого светлого макета
  получил авторскую сцену: одинокое тёплое освещённое окно в тёмном силуэте
  здания ночью — окно горит сразу, не только при наведении. В углу — маленькое
  декоративное созвездие, приглашающее к «Колесу».
- Служебная правка внутреннего экспорта дизайн-системы (не видна на сайте).

**Зачем:** первый экран должен сразу задавать настроение сайта («офисная
готика» — жуткое и чудесное внутри обыденного), а не быть нейтральной заглушкой.

**Технические подробности:** `components/vitrina/WarmWindowHero.tsx`,
`app/globals.css` (namespaced `.warm-hero`, keyframes с гейтом под
reduced-motion), новый акцентный оттенок `--sodium`/`--ring` (тёплая лампа).
Сцена полностью пререндерится на сервере — окно «горит» уже в исходном HTML,
без мигания при загрузке.

**Как проверить:** открыть главную страницу — должно быть видно тёмное здание
с одним тёплым горящим окном и маленьким созвездием в углу; ссылка
«↓ блуждать по Колесу» ведёт к карте ниже.

---

## 2026-06-28 — Внутренняя техническая настройка (не видна на сайте)

**Что изменилось:** расширен служебный экспорт дизайн-системы в инструмент
claude.ai/design (добавлено ещё 7 компонентов сайта) — используется отдельно
от самого сайта, посетители изменений не увидят.

---

## 2026-06-26 — «Мастерская», страница контактов, доступность, скорость, SEO — базовый план завершён

**Что изменилось:**
- Появился раздел «Мастерская» — авторские заметки и материалы отдельно от рассказов.
- Появилась страница контактов автора — тихая, тёмная сцена «офис ночью» в
  конце «Витрины».
- Проведена проверка доступности сайта: усилен видимый фокус при навигации с
  клавиатуры, добавлена ссылка «пропустить к содержимому» для скринридеров,
  проверен контраст текста.
- Ускорена работа сайта: изображения отдаются в современных форматах
  (AVIF/WebP), включена автоматическая оптимизация React, тяжёлые анимации
  подгружаются только когда нужны.
- Настроены метаданные для соцсетей и поисковиков: у страниц появились
  красивые превью-картинки в фирменном стиле при шеринге ссылок, добавлена
  карта сайта для поисковиков.
- Финальная настройка зерна/CRT-эффекта на подобранных «на глаз» значениях.

**Зачем:** это завершающие шаги базового плана сайта (§14 тех-спецификации,
фазы 0–6) — после них сайт считается «построенным», а не проектом в разработке.

**Технические подробности:** `lib/workshop.ts` + `content/workshop/*.md`
(файловый контент «Мастерской», отдельно от `content/stories`),
`app/(vitrina)/contacts/page.tsx`, `content/contacts.ts`;
`components/wheel/WheelIndex.tsx`/`WheelCanvas.tsx` (контрастный фокус);
`next.config.ts` (AVIF/WebP, React Compiler, суженные размеры изображений);
`lib/og.tsx` (динамические картинки для соцсетей), sitemap/robots/JSON-LD;
`docs/definition-of-done.md` — чек-лист §13.

**Как проверить:** открыть «Мастерскую» и страницу «Контакты» в меню сайта;
пройтись по сайту клавишей Tab — должен быть виден чёткий контур фокуса;
отправить ссылку на сайт в мессенджер — должна показаться авторская
превью-картинка.

---

## 2026-06-25 — «Колесо»: доступная карта смыслов и визуальный граф; первый слой «офисной готики»

**Что изменилось:**
- Появилась главная фишка сайта — «Колесо»: карта тем и рассказов. Сначала —
  доступный список «тема → рассказы» (открывается любому, включая
  скринридеры), а поверх него на широких экранах — живой визуальный граф-схема
  со связями, кружками и подсветкой.
- Карта реагирует на наведение и на клавиатурную навигацию: соседние
  темы/рассказы подсвечиваются, остальное отступает на второй план; при
  наведении на рассказ карта плавно перестраивается вокруг него.
- Исправлен ряд найденных при ручном тестировании багов «Колеса»: рассказы
  иногда «вылетали» за пределы кольца тем, наведение на рассказ иногда
  зацикливалось (карта дёргалась туда-обратно), превью рассказа не
  показывалось при выбранной теме, приглушённые рассказы под выбранной темой
  были случайно кликабельны.
- На «Витрине» появились первые сцены с прокруткой: ниже «Колеса» при скролле
  проявляются блоки «Автор / Книги / Мастерская».
- Открытие рассказа из «Колеса» теперь сопровождается короткой вступительной
  анимацией текста (первая строка распадается на слова/буквы и снова
  собирается) — это разовое «событие входа», сам текст рассказа не меняется.
- Появился первый слой «офисной готики»: лёгкое зерно/винтажный эффект
  экрана, необычный курсор мыши, «проявление из тумана» — всё только в
  «Витрине», отключается при «снизить движение» или на слабых/тач-устройствах.
- Несколько правок скорости и плавности спецэффектов по итогам проверки
  (глитч-эффект замедлен, поправлен баг с пропаданием текста при переключении
  «снизить движение»).
- Начат экспорт дизайн-системы сайта во внешний инструмент claude.ai/design
  (внутренний технический процесс, на сайт не влияет).

**Зачем:** «Колесо» — не просто навигация, а «дом» сайта: карта смыслов,
которая должна быть одинаково полноценна и для мыши/клавиатуры, и для
скринридера — по жёсткому правилу проекта «доступность — канон».

**Технические подробности:** `lib/graph.ts` (`buildGraph`/`reweight`),
`lib/wheelLayout.ts` (силовая раскладка d3-force, расчёт на сборке, не в
браузере), `components/wheel/WheelIndex.tsx` (доступный список),
`WheelGraph.tsx`/`WheelCanvas.tsx` (визуальный граф, `next/dynamic` без
серверного рендера — только широкие экраны), `components/motion/Reveal.tsx`
(сцены прокрутки на ScrollTrigger), `components/haunted/ZachinProvider.tsx`,
`StoryOpening.tsx`, `GlitchText.tsx`, `GrainOverlay.tsx`/`GrainCanvas.tsx`,
`CustomCursor.tsx`, `FogReveal.tsx`, `useHauntedCapability.ts` (уровень
возможностей устройства).

**Как проверить:** открыть главную страницу на широком экране — под
«Колесом» должна быть видна живая карта; навести на тему/рассказ — соседние
элементы подсвечиваются; открыть любой рассказ с карты — должна проиграться
короткая анимация первой строки; включить «снизить движение» — зерно/курсор/
анимации должны стать статичными без потери контента.

---

## 2026-06-24 — Контент-пайплайн, токены «офисной готики» и «Читальня»

**Что изменилось:**
- Настроен путь превращения исходных PDF двух книг в контент сайта: 34
  рассказа автоматически извлечены из PDF и приведены к чистому тексту (без
  колонтитулов и номеров страниц), для каждого сгенерирована страница и
  вынесена иллюстрация-плашка.
- Подобраны шрифты и цвета сайта: «книжный» шрифт для текста рассказов,
  отдельный «системный» — для интерфейса; фирменная тёмно-натриевая цветовая
  палитра «офисной готики».
- Заработала «Читальня» — раздел чтения рассказов: список книг и рассказов,
  страница отдельного рассказа с иллюстрацией, переключатель книг,
  постраничный и непрерывный режимы чтения, три темы оформления страницы
  (светлая/сепия/тёмная), сохранение прогресса чтения между визитами
  («Продолжить чтение»).
- Исправлен ряд дефектов «Читальни», найденных при ручной проверке: мигание
  темы оформления при обновлении страницы (F5), сбой высоты «страницы» при
  постраничном чтении, застревание прогресса при переходе между рассказами,
  съезжающие крупные иллюстрации, недоступная в прокрутке шапка навигации,
  слабо заметный фокус с клавиатуры.
- Составлены два трекера для дальнейшей работы: список отложенных задач
  (разметка тем рассказов, ручная вычитка текста и другое, что должен сделать
  человек) и чек-лист ручной проверки в браузере.

**Зачем:** это первый полный проход по контенту сайта — от исходных книг до
читаемых страниц — и закладка визуального языка («офисная готика»), на
который опирается всё остальное.

**Технические подробности:** `scripts/ingest_pdf.py` → `content/_raw/`,
`scripts/build_mdx.py` → `content/stories/*.mdx` + `public/illustrations/`,
`content/schema.ts` (zod-схемы, 10 тем), `lib/content.ts`; `lib/fonts.ts`
(Source Serif 4 + Inter + JetBrains Mono, cyrillic+latin), `app/globals.css`
(OKLCH-токены `--sodium`/`--paper`/`--concrete`/`--monitor`);
`components/motion/MotionProvider.tsx` (единый шлюз reduced-motion), две
route-группы `(vitrina)`/`(chitalnya)`; `app/(chitalnya)/read/**`,
`components/reader/Reader.tsx`, `ReaderShell.tsx`, `reading.ts`; анти-FOUC
пре-пейнт скрипт в корневом `layout.tsx`.

**Как проверить:** открыть «Читальню» (`/read`), выбрать книгу и рассказ —
текст и иллюстрация должны отобразиться без миганий при обновлении страницы
(F5); переключить тему чтения и режим (постраничный/непрерывный) в
настройках страницы рассказа.

---

## 2026-06-23 — Заложен фундамент проекта

**Что изменилось:** создан технический каркас сайта (Next.js, React,
TypeScript, Tailwind) и записаны основные документы проекта — инструкция для
ИИ-помощника, концепция сайта и техническая спецификация.

**Зачем:** первый день проекта — подготовка инструментов и договорённостей,
на основе которых строится всё остальное.

**Технические подробности:** `CLAUDE.md`, `docs/концепция.md`,
`docs/тех-спецификация.md`; каркас Next.js 16 (App Router) + React 19 +
TypeScript 5 + Tailwind v4, базовые библиотеки и структура папок
(`app/(vitrina)`, `app/(chitalnya)`, `components/{wheel,reader,motion,haunted,ui}`,
`content/`, `lib/`, `scripts/`).

**Как проверить:** нет пользовательской проверки — правки этого дня не
создают видимых страниц, только основу проекта.
